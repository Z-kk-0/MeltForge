export struct FileEntry {
    path: string,
    selected-format: string,
    available-formats: [string],
    is-image: bool,
    image: image,
}

export struct ToastMessage {
    id: int,
    kind: string,
    title: string,
    message: string,
    created_at_ms: float,
    lifetime_ms: float
}
